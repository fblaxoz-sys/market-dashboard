# Kept lean to fit Render's 512MB free tier. sklearn's HistGradientBoosting
# replaces XGBoost/LightGBM/CatBoost (nearly as accurate, far less memory).
# Shared portfolio tracker (tracker.html) — Postgres-backed shared state.
psycopg[binary]>=3.1
statsmodels>=0.14.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
