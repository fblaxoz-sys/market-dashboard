services:
  - type: web
    name: market-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python proxy_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
